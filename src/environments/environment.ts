export const environment = {
    production: true,
    // TODO: Đặt URL API production thực tế trước khi deploy (hiện đang trỏ localhost).
    apiUrl: 'http://localhost:8080/api/v1',
    siteName: 'LUNCHORDER - Angular'
};